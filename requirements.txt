# Core backend
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
pywebview>=4.4

# Vision / video / imaging
opencv-python>=4.9
pillow>=10.0
numpy>=1.26

# LLM HTTP calls
requests>=2.31

# Optional teacher detection + skeleton motion analysis (YOLO / YOLO-pose) is
# NOT installed by default because it is heavy. Installing ultralytics enables
# both automatic teacher cropping AND shoulder-normalized wrist tracking for the
# motion pre-filter (plan.v2 §2-3). Without it the app still works: cropping
# falls back to the full frame and the motion pre-filter falls back to a coarse
# frame-difference estimate.
#   pip install -r requirements-yolo.txt
